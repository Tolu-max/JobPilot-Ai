# Example Profile

Copy this folder to create a local profile:

```bash
copy profiles\example\preferences.json profiles\main\preferences.json
```

Then add your resume locally and update `resumePath`. Real resumes and real profile folders are ignored by default.
