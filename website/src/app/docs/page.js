import DocsClient from './DocsClient';

export const metadata = {
  title: 'Documentation | JobPilot',
  description: 'Learn how to install, configure, and run the local-first JobPilot CLI.',
};

export default function Docs() {
  return <DocsClient />;
}
